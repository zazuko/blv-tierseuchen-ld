image: zazukoians/node-java-jena
before_script:
  - npm install
  - curl https://ipinfo.io/ip

stages:
  - build

pipeline_build:
  stage: build
  script:
    - npm run start
  artifacts:
    paths:
      - target/everything.nt
    expire_in: 1 day
  # except:
  #   variables:
  #       - $CRON

# tierseuchen_staging:
#   stage: staging
#   script:
#     - npm run clean-nt
#   only:
#     variables:
#         - $CRON

